import 'package:flutter/material.dart';

const Color gren = Color.fromRGBO(204, 255, 0, 1);
const Color dgren = Color.fromARGB(144, 170, 219, 1);
const Color blu = Color.fromRGBO(144, 170, 219, 1);
const Color blac = Colors.black;
const Color wit = Colors.white;

const double defaultPadding = 16.0;
const Duration defaultDuration = Duration(milliseconds: 700);
const Curve defaultCurve = Curves.easeInOutCirc;
