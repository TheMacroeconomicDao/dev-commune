import 'package:flutter/material.dart';

const Color login_bg = Color.fromARGB(255, 50, 99, 172);
const Color signup_bg = Color.fromARGB(255, 0, 69, 133);

const double defpaultPadding = 16.0;
const Duration defaultDuration = Duration(milliseconds: 700);
